<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!-- Application phụ thuộc Domain để handler và DTO làm việc với entity/value rule cốt lõi của hệ thống chat. -->
    <ProjectReference Include="..\ChatApp.Domain\ChatApp.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- FrameworkReference này mở các abstraction DI/config cơ bản để DependencyInjection.cs có thể dùng IServiceCollection. -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
